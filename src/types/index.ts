export interface UserType {
  id: number,
  firstName: string,
  lastName: string,
  email: string,
  gender: string,
  zip: number,
  birthdate: string,
  city: string
}

export interface UserItemProps {
  name: string;
  email: string;
};
